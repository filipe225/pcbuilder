
extends ../template.pug 

block content
    .display_header
        h2 Processor's List from database
        a(href="/admin/pccase/add_pccase") Add New
    table.styled-table
        thead
            tr
                th Id
                th Name
                th
                th
        tbody
            each pccase in all_pccases
                +table_list(pccase)